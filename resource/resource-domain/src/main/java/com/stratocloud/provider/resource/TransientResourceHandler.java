package com.stratocloud.provider.resource;

/**
 * Transient resource means it can be deleted when it's not found from cloud provider.
 */
public interface TransientResourceHandler extends ResourceHandler {
}
